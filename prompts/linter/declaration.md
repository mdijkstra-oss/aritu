Each unit is one named declaration — a function, method, type, constant, variable or field — identified by its name as written in the source, a method or field as Type.Name.

Everything declared inside a function body is part of that function's unit: judge a local variable, a parameter or a nested function under the declaration enclosing it, not on its own.
