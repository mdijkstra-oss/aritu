Judge each unit in <units> against the rule in <rule>. Judge the unit as written on the left of its arrow; the key on the right is only where the answer goes. Answer under exactly the keys the schema names.

Fail a unit only on what the files above prove. A declaration these files never use may be used by files you were not given — being unreferenced here is never a failure.
