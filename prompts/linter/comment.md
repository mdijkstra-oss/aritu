Each unit is one comment, identified by the declaration it documents or sits inside — a method's as Type.Name, one belonging to no declaration as (file). Where a name carries a #01 suffix, the units under that name are the comments in the order they appear in the file.

Judge the comment, not the code it sits on. The declaration is context for deciding whether the comment earns its place, and never itself the thing that passes or fails.
