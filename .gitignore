# Built binary. Anchored to the repo root so it cannot swallow cmd/aritu/.
/aritu

# Test and coverage output
*.test
*.out

# Editors
.idea/
.vscode/
*.swp

# macOS
.DS_Store

# JetBrains
.idea/

# Claude Code. The skills are checked in so they follow the repo into a
# worktree; the worktree checkouts and per-machine permissions do not.
.claude/worktrees/
.claude/settings.local.json
